---
- name: Connection Check
  hosts: all
  tasks: 
    - name: Ping the server
      ping:

- name: Setup Infrastructure
  hosts: all
  become: yes
  roles:
    - incus-setup
    - sqlite-setup
    - etcd-setup
    - coredns-setup