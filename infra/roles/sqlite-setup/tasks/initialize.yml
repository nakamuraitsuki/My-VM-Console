---
- name: Check if database exists
  ansibel.builtin.stat:
    path: "{{ db_path }}"
  register: db_stat

- name: Initialize database schema
  ansible.builtin.shell:
    cmd: "sqlite3 {{ db_path }} < {{ role_path }}/files/init.sql"
  when: not db_stat.stat.exists