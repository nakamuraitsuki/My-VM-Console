---
- name: Install sqlite3 client
  ansible.builtin.apt:
    name: sqlite3
    state: present

- name: Ensure DB directory exists
  ansible.builtin.file:
    path: "{{ db_dir }}"
    state: directory
    mode: '0755'
    owner: root
    group: root